Base Dev Environment for wordpress 
nginx
php 8.3
mysql 

create .env file

DB_NAME=<db_name>
DB_USER_NAME=<user_name>
DB_USER_PASSWORD=<password>
DB_ROOT_PASSWORD=<password>

run docker compose up to create dev environment
